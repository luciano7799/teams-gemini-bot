services:
  - type: web
    name: teams-gemini-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: TEAMS_SECURITY_TOKEN
        sync: false
